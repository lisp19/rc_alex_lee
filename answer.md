# 投递系统设计决策记录

## 1. 系统边界

### 1.1 本系统解决哪些问题？

系统定位：对多种外部供应商API接口进行聚合，proxy和集中接入的代理层服务，不感知业务细节，只做请求转发和可靠性保证。

- **负责从通知成功持久化开始的可靠投递。**  
  请求入库后，系统负责异步调度、HTTP(S) 投递、失败重试、状态记录和进程异常后的恢复。

- **负责统一封装不同外部 Target 的调用方式。**  
  通用场景通过配置完成 URL、Header、Query、Body、鉴权和结果判定，特殊逻辑可通过 SDK Adapter 扩展。

- **负责基础幂等、失败处理和人工重试能力。**  
  上游提交支持幂等键，下游可按配置注入幂等键，最终失败任务保留状态并支持受控重试。

- **负责最小化资源控制和运行观测。**  
  基础的链路可观测性和配额限流控制。

### 1.2 本系统明确不解决哪些问题？

- **不保证供应商内部业务执行成功。**  
  系统默认以 HTTP 层投递结果为成功边界，业务返回码只通过可选 Hook 做有限解析。
- **不保证请求只执行一次。**  
  网络超时、响应丢失等场景可能导致重复调用，下游幂等能力由 Target 配置显式声明。
- **不承诺通知顺序。**  
  MVP 不实现全局顺序或同一业务对象的严格有序投递。
- **不覆盖调用方业务事务与通知提交之间的一致性。**  
  系统责任从通知成功持久化开始，上游业务事务和通知请求之间的原子性由调用方自行处理。不感知业务逻辑。

---

## 2. 可靠性与失败处理

### 2.1 采用什么投递语义？

- **采用至少一次触发语义。**  
  因此系统保证请求至少被投递一次，同时接受重复调用可能。

- **通过幂等配置降低重复调用风险。**  
  支持在 Header、Query 或 JSON Body 中注入稳定幂等键，并允许按 Target 配置是否支持安全重试。

### 2.2 外部系统失败时如何处理？

- **按默认 HTTP 规则和可配置 Hook 判断是否重试。**  
  2xx 默认成功，408、429、5xx 和部分网络错误默认可重试，其余结果按配置进入失败或终止状态。

- **自动重试采用有限周期和延迟队列。**  
  Retry Policy 定义重试间隔和最长自动重试窗口，RabbitMQ 负责重试调度，DB 保存任务状态。

- **最终失败任务保留并支持受控重试。**  
  自动重试耗尽后任务进入 failed 状态，可通过查询接口定位，并由管理操作重新触发。

### 2.3 外部系统长期不可用时如何处理？

- **不进行无限重试。**  
  每个 Target 或调用方可配置最大自动重试周期，达到截止条件后停止自动投递。

- **业务有效期由调用方负责。**  
  系统只执行配置的 Retry Policy，不判断一条通知在业务上是否已经失效。

- **系统恢复后允许运维介入。**
  预留人工介入接口允许系统恢复后人工恢复指定任务。

---

## 3. 中间件选型与替代方案

### 3.1 持久化存储

- **MariaDB 作为持久化存储。**  
  任务记录、状态和配置版本均落库，任务最终状态只以数据库为准。

- **选择原因。**  
  商业License友好、MySQL生态兼容性、项目启动轻量部署调试。

- **替代方案。**  
   PostgreSQL/MySQL/云上RDS/其他支持关系查询的DBMS。

### 3.2 RabbitMQ

- **RabbitMQ 负责异步调度和重试消息传递。**  
  实现系统中的异步调度和重试。

- **选择原因是 。**  
  AMQP协议支持完善、轻量便于mvp版本早期开发、既能快速部署又能集群部署生产使用。

- **替代方案。**  
  Kafka/Rocket MQ/云上event bus等。

### 3.3 Redis

- **Redis 仅承担轻量共享状态。**  
  限流/轻量级锁。

- **选择原因。**  
  轻量级高速缓存存储、分布式锁。

- **替代方案：进程内限流和数据库 Lease。**  
  单实例或低规模环境可以直接移除 Redis。

---

## 4. 第一版主动控制的复杂度

### 4.1 哪些设计在第一版被认为过度？

- **严格顺序投递暂不实现。**  
  当前需求没有统一顺序约束，提前引入 partition key、串行 Worker 和分区调度会增加明显复杂度。MVP版本不做严格顺序。
- **Exactly-once 不作为设计目标。**  
  外部 HTTP 副作用无法参与本地事务，继续增加内部锁和去重机制无法消除网络不确定窗口。MVP版本优先保证基础功能不做严格一次执行语义。
- **完整管理平台暂不建设。**  
  第一版通过配置文件、管理库和脚本完成 Target、Retry、Quota 和 Hook 管理，不做完整管理后台服务。
- **复杂流量治理暂不引入。**  
  MVP 只提供基础 Quota 和并发限制，不做复杂流量治理。
- **完整可观测性平台暂不绑定。**  
  第一版输出结构化日志和关键运行状态，后续可接入企业现有监控体系。

---

## 5. 后续演进

- **各个模块可分离部署&独立扩缩容。**  
  MVP版本单体服务但各个组件有明确界限，后续可按需拆分API，Worker等模块。

- **Target 数量增长后增加配置平台。**  
  可继续演进管理 API、Web UI、审批、灰度发布、Secret Manager 和 Hook 在线测试。

- **任务规模增长后增强调度能力。**  
  当固定 Retry Queue 无法满足大量长周期定时任务时，可拆出独立 Retry Scheduler。

- **完整多租平台隔离。**  
  可按 Target 或租户引入独立 Consumer Pool、Queue Partition、以及动态配额限制，并在数据库物理存储级别隔离或支持自定义存储。

- **进入正式生产体系后接入标准可观测性组件。**  
  现有结构化日志和可观测性暴露接口可继续对接 Open Telemetry、Prometheus 或企业内部平台。

---

## 6. AI 使用说明

### 6.1 AI 在哪些关键地方提供了帮助？

- **方案选型对比和边界case评估。**  
  对数据库任务队列、MQ、Transactional Outbox、Lease、Redis Lock 等方案进行了对比，重点分析进程退出、重复消息和 HTTP 结果不确定等场景。

- **整理接口契约和配置模型。**  
  对单条/批量提交、Proxy、Target、Retry Policy、幂等键注入和 CEL Hook 等能力进行了结构化梳理，并用于完善 OpenAPI 和配置设计。

- **方案整理和编码实现。**
  基于开发者划清的边界和澄清的业务目标，整理细化设计方案并完成编码实施。  

- **设计文档和实现一致性自审。**  
  在代码形成后继续检查 README、设计方案、部署方式、测试覆盖和实际实现之间的一致性，并压缩不必要的平台化设计。

- **帮助补充故障与验收场景。**  
  围绕 MQ/Redis 中断、Worker 退出、Lease 恢复、配置热更新和多实例执行等情况补充了测试与验收思路。

### 6.2 AI 给出过哪些没有采纳的建议？

- **没有采用纯数据库任务表作为最终调度方案。**  
  早期方案可以用数据库轮询完成 MVP，最终为了生产环境的 ACK、Worker 解耦和延迟重试能力，引入 RabbitMQ 并保留数据库为事实源。
- **没有采用 NATS 作为最终 MQ。**  
  方案评估阶段考虑过 NATS JetStream，最终选择 RabbitMQ，保证生产环境的可用性和生产/开发阶段的一致性。
- **没有把任意脚本作为响应扩展机制。【过度设计】**  
  原始设计的扩展需求允许业务自定义解析逻辑，基于人工收敛，最终收敛到受限 Hook，以控制安全、资源和运行时风险。
- **没有提前实现顺序、Exactly-once 和完整管理平台。**【过度设计】  
  这些能力会显著扩大系统边界，当前需求不足以证明其 MVP 必要性，因此只保留可演进的模块边界。人工剔除相关设计。

### 6.3 哪些关键决策由本人作出，以及原因？

- **将责任边界确定为“通知成功持久化之后”。**  
  这样系统能够对自身可控制的可靠性负责，上游业务事务和通知提交之间的一致性保持清晰边界。
- **将 HTTP 送达确定为系统核心成功语义。**  
  通知系统保持通用性，默认根据 HTTP 结果判定，并通过 Hook 为少数业务协议提供有限扩展。
- **采用至少一次触发并显式建模下游幂等能力。**  
  该模型能够覆盖 HTTP 不确定结果和进程恢复场景，同时避免对外部副作用作无法证明的 Exactly-once 承诺。
- **采用无状态单体进程和 Role 模块化。**  
  当前部署保持简单，同时允许通过 `api,worker,outbox,recovery` 角色在未来独立扩缩容。
- **管理面和业务面分离。**  
  管理数据如白名单、业务配置等库和业务运行时依赖的任务库分离，运行时进程对管理面数据只读监听。

---

## 7. 实现过程中的工程总结

### 7.1 实际开发过程中最重要的一次设计调整

- **将“MQ 负责可靠性”收敛为“数据库保存事实，MQ 负责调度”。**  
  最初版本采用http触发投递，mq保证可靠性的思路。最终实现通过 Notification + Transactional Outbox 保存权威状态，RabbitMQ 消息可以重复或重建，Recovery 也能够从数据库修复缺失调度。

这一调整直接影响了后续的 Worker Claim、generation、Lease、Outbox Publisher Confirm 和 Recovery 设计，使 RabbitMQ 故障、重复消息和应用重启都能落到同一套数据库状态机处理。

### 7.2 当前实现中最希望评审关注的工程取舍

- **可靠性机制形成了可验证的完整闭环。**  
  请求持久化、Outbox、MQ、Worker Claim、HTTP 调用、Attempt、Retry 和 Recovery 之间都有明确状态转换，核心状态最终回到 MariaDB。
- **代码保持单体部署，同时保留清晰模块边界。**  
  同一 Binary 已拆分 API、Worker、Outbox 和 Recovery 角色，可在当前 MVP 中减少部署复杂度，也可以直接映射为未来的独立 Deployment。
- **验收覆盖真实故障路径。**  
  仓库除单元和冒烟检查外，还提供多实例、本地真实 RabbitMQ/MariaDB/Redis/Mock Target，以及 Worker 退出、MQ/Redis 中断、热更新等 E2E 场景。

### 7.3 如果继续投入一天时间，优先补充的能力

- **补充更完整的可观测性建设。**  
  在现有结构化日志和 Health 基础上增加 Prometheus/Open Telemetry 指标适配和最小化部署，可以更直接地观察投递延迟、重试、积压、和 Target 错误率，是进行真实部署的前提。

- **优先补充基于真实运行数据的性能和容量基线。**  
  当前实现已经覆盖功能正确性和主要故障恢复路径，发布MVP版本前需要进行基础的全链路压测和容量评估。

- **业务协议拓展。**  
  首日版本优先保障基础功能，对client侧协议较为简单，后续建议拓展协议提供更多可定位的状态码、轮询接口等便于真实接入。
